displayName: JWT Validation Middleware (no-auth for preflight)
type: middleware
import: github.com/alexpetul/jwt-validation-middleware
summary: 'Verify JWT Token in Auth header, Cookie or Query param, and injects decoded payload in header'

testData:
  secret: ThisIsMyVerySecret
